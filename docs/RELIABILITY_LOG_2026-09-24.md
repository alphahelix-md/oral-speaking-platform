# Oral 无人值守开发日志 — 2026-09-24

## 范围与时间
- 窗口：2026-09-24 09:38–12:38（Asia/Shanghai，UTC+8）。
- 授权：自主开发、测试、推送 `codex/cloudbase-run-test`；不改 main，不 force push；保留用户已有文件。
- 规则：不向用户提申请；每项故障最多重试两次，仍失败则记录并跳过。登录、部署、采购、新凭据留待用户处理。
- 禁止：关机、输出密钥、重新收费筛查模型、启用未验收备用。

## 恢复与已完成检查
- 已完整读取指定交接（实际目录为 `.codex/handoffs/`）、AGENTS.md、ROADMAP.md；执行 Next Command。
- 实际分支 `codex/cloudbase-run-test`，HEAD `8eb03ab`，与交接一致。用户既有 `.gitignore`、ROADMAP.md、package.json 及未跟踪研究/部署文件保留。
- 技能：session-handoff、caveman、skill-portfolio；阅读 Next.js 本地客户端文档。
- 默认终端与 Node 沙箱启动失败；已授权的沙箱外终端可用。apply_patch 同类基础设施错误；使用精确 Git 补丁，先 `git apply --check` 再应用。

## 09:38 检查点
- 已实现（待浏览器验收）：原始 Blob 先存及 SHA-256 读回，独立派生 WAV；持久草稿与恢复入口；稳定 Turn ID；答案与下一题原子快照；STT 分片及评价请求前检查点；结果不明不重发，允许保留录音跳过转写。
- 已通过：81 项测试 / 16 文件、类型检查、Next.js 16.3.5 生产构建。
- 新增本地 headless Edge 浏览器故障测试脚本；API 全部模拟，收费请求为 0。
- 未完成：浏览器脚本执行、真机验证、服务器端跨设备幂等/预算账本、部署。

## 09:40 浏览器测试首次失败
- 原因：测试在 Next.js hydration 前点击服务端 HTML，未进入练习选择；不是已确认的产品故障。
- 重试 1：增加 React 挂载后的 history 状态等待；失败时保存截图与页面文本。
- 本机 next start 已启动；提示 standalone 建议入口，服务健康。

## 09:44 浏览器重试 1 挂起
- 已进入练习并保存草稿，刷新后页面停留首页；CDP Promise 未返回。只读诊断已确认浏览器存活。
- 停止本测试进程（PID 26204），未触及用户浏览器。
- 重试 2：为每个 CDP 调用加 10 秒超时与步骤日志；若仍失败，保存证据并跳过完整浏览器套件。

## 09:43 浏览器套件暂跳过（已用满两次重试）
- 第二次重试在交互选择器点击时失败：元素检查后节点已消失。证据：.codex/qa/reliability-1790214176581/failure.txt、failure.png。
- 不把浏览器套件算通过；继续自动故障测试与代码审查。后续浏览器/真机验证留待人工处理。
- 诊断辅助命令曾因 CommonJS 顶层 await 语法失败，已修正并只读成功。

## 09:50 实际处理函数故障测试
- 90 项测试 / 17 文件通过，覆盖真实 finishRecording、submitAnswer、finishSession 的保存顺序、存储满、快速双击、读回失败、评价中断及锁释放；不是完整浏览器测试。
- 审查发现新 STT requestId 格式与服务器正则不匹配，已改成稳定 sp_ 格式；避免被服务器改成随机 ID。
- 修复练习处理中切换语言，以及派生回放缓存读取失败遮蔽原音的问题。
- ROADMAP.md 已最小同步旧状态并追加本轮边界；用户原稿备份 .codex/qa/ROADMAP.before-reliability.md，本轮不暂存该用户已有文件。
- 一个编排工具调用因括号语法错误未执行，修正后成功；无文件副作用。

## 09:52 首批提交准备
- 90 项测试、类型检查、生产构建通过；git diff --check 无错误。
- 部署与手机故障矩阵未执行，保留为人工任务。回滚基线 8eb03ab。
- 仅暂存本轮源码、测试和新文档；ROADMAP.md（含旧用户修改）、.gitignore、package.json、RESEARCH 和已有部署产物不纳入提交。

## 09:54 构建故障与重试 1
- 更正上一条：初次生产构建通过，但最新回归构建失败，原因是 Tailwind 扫描到测试 Chromium profile 的锁文件。不是 TypeScript 错误。
- 仅停止本任务隐藏 Edge，校验绝对路径后删除本任务创建的三个 profile；截图和失败文本保留。
- 测试脚本的临时 profile 改用系统临时目录，避免污染源码扫描；没有再次运行浏览器套件。
- 重新执行生产构建；仍最多两次重试。

## 09:55 构建重试 2
- 第一次清理的进程筛选未匹配 Edge 的实际参数格式，留下锁文件，构建重试 1 仍失败。
- 已按本任务三个唯一 profile 标识精确停止残留测试进程，再校验路径后清理；没有停止用户浏览器。
- 执行最后一次构建重试；失败则记录为发布阻塞。

## 09:57 首批构建恢复
- 清理本任务临时浏览器 profile 后生产构建成功。90 项测试及类型检查通过。
- 远端 fetch 成功；HEAD 与 origin/codex/cloudbase-run-test 均为 8eb03ab，无并行新提交。
- 浏览器套件仍标记未通过，未额外重试。

## 10:00 下一阶段：免费补救与边界修复
- 首批 db50977 已推送独立分支，未部署。
- 增加保存失败时的文字/草稿 JSON 导出，标明不包含音频且不等于平台已保留；原音单独下载。
- 修复用户手动改稿被后续分片转写覆盖；认证/本平台额度等明确未进入 Provider 的拒绝允许修正后重试，未知或已发出的请求仍不重发。
- TTS 本机缓存读写失败不再丢弃已收到的音频；原音恢复增加 MIME/长度收据核对。
- 检查训练上传已规范化 MIME（移除 codecs 参数），兼容原始 WebM/MP4；无新上传权限。

## 10:30 请求预算实现与验证
- 已新增现有 Upstash REST 后端的原子 Lua 预算预留：账号/全站/Session 的请求、音频时长、估算金额；同一操作的并发重复请求拒绝，内部第二次尝试单独计数。
- GLM 每个实际 fetch（含语言纠错重试）通过预算边界；转写超时设为每次 20 秒。Evaluation 的路由重试也逐次预留。
- WAV 时长根据 PCM 字节推导，单片最多 25 秒，最多 12 片；拒绝伪造或不支持的格式。
- 删除旧按整段录音去重的非原子额度逻辑；更正旧额度文案。
- 109 项测试 / 20 文件通过；本机没有 Redis/Lua 运行时，真实原子脚本集成待人工环境验证。没有配置任何线上预算、凭据或价格，没有额外调用模型。
- 技术依据：Upstash REST API 官方 POST 命令文档及 Redis EVAL 官方文档；未创建临时数据库。
- 两次只读文件定位遇到不存在的 provider 路径，已读取真实路径，无写入副作用。
- 上次日志追加因 PowerShell 中文引号解析失败，本条补记；改用单引号 here-string。

## 10:35 路由验收与存储拒绝边界
- 116 项测试 / 21 文件通过，新增真实 /api/transcribe 与 /api/ai 路由测试；Provider 和 Redis 均为模拟，无收费调用。
- 额外审查发现偏好设置直接写 localStorage 可能在全局存储满/禁用时抛错，使导出补救界面不可用；已改为可失败的偏好存储，核心 Session 保存仍严格报错。
- 补充预算配置文档 docs/PROVIDER_BUDGETS.md，明确当前部署尚未启用、估算金额不等于真实账单、服务端防重仅七天窗口、TTS 不在新增付费预算内。
- 本阶段新增的 API 路由行为已阅读 Next.js 本地 Route Handlers 文档；没有新增依赖或迁移线上数据。

## 10:43:49 状态标记与阶段验收
- 自动审批审核曾超时，命令未执行；按提示重试一次后成功，不需要用户干预。
- 已保留提交后的分片状态，区分纯文字、跳过/部分转写与删除录音；保存冲突提供先导出再刷新的路径。
- 119 项测试、类型检查通过；新增拒绝空白证据评价的路由检查，避免无意义收费。
- 已创建 docs/ALPHA_ACCEPTANCE.md，所有真机和真实 Redis 项目明确待验，未制造样本或通过率。

## 10:45:50 第二批提交准备
- 120 项测试 / 22 文件、类型检查、最新生产构建通过。
- 本批包含免费导出、手动改稿保护、偏好/缓存降级、每题阶段记录、服务端分片/重试预算代码及说明。
- 真实 Redis 原子性、真机刷新与回放、50 样本和三天指标仍待验；正式金额硬上限不能由估算替代。
- 没有启用线上预算、备用模型、云同步或新增凭据。

## 10:59:42 录音系统中断修复
- 第二批 1d57d03 已成功推送独立分支；部署仍为用户确认的 8eb03ab。
- 录音器从启动时接收结束事件，缓存最终原音；错误后等待最后数据事件，系统结束后自动进入原音保存和读回。不会自动向模型发送中断音频。
- 明确停止的并发请求共享结果；暂停时间不计入时长；卸载后不触发自动保存，迟到的麦克风授权会释放。
- 修复麦克风权限等待期间离开页面后，录音未进入停止流程的问题。
- 录音及实际停止处理函数共 18 项故障测试通过；未运行已耗尽重试的完整浏览器套件。
- 一次只读文件定位使用不存在的 normalize.ts 路径；不影响更改，无文件副作用。

## 11:04:15 存储损坏边界审查
- 127 项全量自动测试、类型检查与生产构建通过后，追加发现的两处数据保护修复。
- 删除记录先严格读取完整历史；损坏 JSON 不再被当空列表写回，删除结果增加读回核对。关联音频删除前遇到历史损坏立即停止。
- 原音首次保存也校验持久化的哈希收据，避免音频字节存在但校验元数据丢失仍标记成功。
- 学习记录与音频跨 localStorage/IndexedDB 的删除仍非原子事务；本轮未扩展数据库迁移或多标签页并发协议。
- 一次只读测试文件定位名称有误，随后列目录定位到 recovery.test.ts；无写入副作用。

## 11:16:03 Redis 人工验收准备
- 已新增 scripts/test-budget-redis.mjs；从真实生产 TypeScript 提取 Lua，测试重复操作并发、九种范围/指标上限、逐次重试计数及 TTL，共 12 类场景。
- 脚本仅在人工提供独立测试库环境变量且显式 --run 时联网；随机测试键前缀，清理仅删除本轮枚举键；不加载 .env.local，不调用模型。
- node --check 与 --check 离线提取通过，网络请求为 0。真实 Redis 检查仍待用户执行。
- 131 项自动测试通过。一次合并验证命令的自动审批审核超时，未执行；已按提示重试一次类型检查并通过，构建另行执行。

## 11:17:30 第三批验收与提交准备
- 131 项测试 / 22 文件、类型检查、生产构建、git diff --check 通过。
- Redis 脚本无参数运行按预期拒绝，未联网；这是保护验证，不是执行失败或后端通过。
- 本批只提交录音中断、存储损坏保护、相应测试与验收文档；ROADMAP.md、package.json、.gitignore 及用户部署产物继续留在工作区。
- 真机/真实 Redis 属于人工环境待办；不启动备用模型、数据库迁移或下一 Phase。

## 11:22:52 无人值守代码交付检查点
- 第三批 3dfeac003a02aae8297e6d3efc4efaa512244f8a 已推送；ls-remote 验证远端独立分支与本地完全一致。
- 本轮共三批功能提交：db50977（原音/草稿）、1d57d03（免费补救/预算源码）、3dfeac0（系统中断/存储保护/实测脚本）。
- 已停止仅本轮创建的 localhost:3417 测试服务器及其 Node 子进程。此前隐藏测试浏览器已清理；未停止用户浏览器或关机。
- 没有操作 main、部署、登录、采购、安装依赖、创建凭据、调用收费模型或开启备用。源码推送不等于已上线。
- 用户原有未提交文件保留；ROADMAP.md 只追加/校正本轮状态，仍不纳入提交。新交接将保存至 .codex/handoffs/，不改原交接。
- 本轮可离线完成的可靠性工作已收敛；剩余门槛依赖用户部署、手机与独立 Redis 环境，记录后停止扩展，不提前进入新 Phase。
- 回来后先部署独立分支，再按 docs/ALPHA_ACCEPTANCE.md 验收原音刷新/中断、草稿、双提交、无模型手动闭环；预算强制模式须先完成人工配置和真实 Redis 验证。

## 2026-09-24T12:42:07+08:00 用户验收与未完成练习入口
- 用户已部署 f7b88af，线上首页/7 个脚本检查检出本轮四个恢复特征。
- 用户确认纯文字草稿刷新恢复后题目一致、文字完整；此结果限当前设备，浏览器/设备名称和内部 Turn ID 尚未核对，不扩大为全部设备通过。
- 用户截图反馈全部历史未完成项堆在首页。已改为介绍卡片下一个未完成练习数量入口，点击进入独立完整列表；保留全部旧记录及原恢复函数，移除重复的继续上次卡片，四种界面语言同步。
- 131 项测试 / 22 文件、类型检查、生产构建通过。四语言组件渲染检查：37 条草稿对应主页一个入口、独立列表 37 条，零草稿不显示入口。
- 追加日志和本地预览启动分别发生一次自动审核超时，均按提示重试一次成功；浏览器工具初始化因 Windows sandbox helper 失败，视觉检查未完成，未重跑旧浏览器故障套件。
- UI 修改待重新部署；部署后先检查主页和列表，再继续原音恢复、重复提交等验收。没有修改存储结构或自动清除历史。
