# ORAL 午后无人值守日志 — 2026-09-24

## 2026-09-24T13:03:54+08:00 启动与用户验收
- 授权窗口：上海时间 12:58:52 至 14:58:52；不向用户申请，故障最多两次重试后记录跳过。
- 用户确认新入口正常、录音停止/处理后刷新恢复可完整播放。当前部署业务基线 2aeac3e，设备/浏览器名称未确认；不推算所有设备或整场完成且保留率。
- 实际分支 codex/cloudbase-run-test；本轮只在该分支开发、测试、提交、推送。部署、采购、登录、新凭据留待用户，不关机，不输出秘密。
- 保留 .gitignore、package.json 及八份用户/并行文档修改，基线哈希位于 .codex/qa/unattended-pm-baseline.json；不整体暂存这些文件。
- 最新计划新增英语六模块规范，但 V0 可靠性仍是前置；不因此开启新模式、备用、专业评分或云同步。
- 已核对具体请求路径：客户端 STT 无截止时间；评价超时只覆盖响应头，响应体卡住仍可使处理状态无限等待。优先修此实际 P1 故障边界。
- 真实 Redis、其他设备、50 次样本和三天门槛无法由本轮离线自动测试替代，继续待验。

## 13:10–13:17 请求截止与旧录音保护
- 客户端统一截止时间覆盖异步认证、响应头及正文；转写 60 秒、评价 25 秒，超时释放界面锁且不自动重发。首轮针对性 18 项通过。
- 首次类型检查发现异步闭包对可空 current 的收窄失效，改为捕获已保存的不可空快照，随后复验。
- 旧录音重试入口原本只保存成功文字，刷新/连点可重发；新增 IndexedDB 同事务领取分段、持久请求 ID、未知结果阻止重发及点击锁。成功旧文字兼容迁移，不删除录音。
- 元数据保存不再把“录音已不存在”当成成功；条件冲突中止事务。
- 两次读取使用不存在的猜测路径（lib/audio-storage.ts、core/session/types.ts），改为已发现的 core/audio/store.ts、types/speaking.ts；无文件修改。vitest 配置实际为 .mjs。

## 13:18–13:24 预算一致性与服务器截止
- 类型检查通过；完整离线回归 24 文件、148 项通过。
- 路由原先主模型两次再备用一次，与硬预算最多两次冲突。统一共享上限为 2：无备用时主模型最多两次；将来获准备用时主/备用各一次；本轮仍不配置备用。
- 新增可取消的整体截止：评价每次最多 10 秒、整体 21 秒，转写整体 45 秒；预算预留、账目写入、模型请求及正文共享信号。过期的预算结果不能再启动模型。
- 首次生成补丁被 Windows 默认 GBK 读取中文源码阻止，尚未写入任何文件；显式 UTF-8 后第一次重试成功。

## 13:25–13:31 本批复核
- 路由故障注入通过；最终账目 HSET 改为有时限的尽力写入，不阻塞已收到模型结果。预留记录为权威计费凭证，完成标记中断仍保留 reserved，不能当作免费调用。新增记账卡死回归通过。
- 24 文件、158 项完整测试通过；类型检查、生产构建通过；Lua 离线提取校验通过，网络调用 0。构建后又补了记账及回放错误处理，提交前再次统一构建。
- 历史录音已知“请求未发送”的认证/访问码拒绝保留安全重试；未知网络结果仍阻止重发。
- 已读回验证的原音不再被可选播放缓存读取失败标成丢失；历史答案回放的存储/播放拒绝显示提示并释放临时 URL。
- 审阅发现现有 Session localStorage 修订号保护能拒绝陈旧页，但不构成两个标签页同时写入的原子事务；保留为待解决 V0 限制，不将旧录音的 IndexedDB 原子领取误表述为全站跨标签原子保护。

## 13:32–13:34 认证迟到复核
- 将整体截止起点前移到服务器认证/上传解析之前，并传递客户端取消信号。迟到认证或预算响应不能在用户已超时后启动收费模型。
- 新增“认证卡住后恢复”“请求进入时已取消”路由测试；针对性 26 项通过。没有线上请求、凭据或配置变更。
- 本轮全部改动只涉及 V0 可靠性；备用、模式、依赖和服务开关均未新增。

## 2026-09-24T13:34:57 本批提交验证
- 24 个测试文件、163 项通过；TypeScript 与最终生产构建通过；git diff --check 通过。
- 本轮没有调用收费模型，没有改变部署、主分支、环境配置或依赖；8 个原有已修改文件哈希全部一致。
- 接下来只暂存本轮可靠性文件，提交并推送 codex/cloudbase-run-test。

## 13:35–13:50 第二批：验收口径与多窗口
- 首批提交 04f87b697714a4b5bf4b619151184a29395930bc 已推送远端独立分支，ls-remote 一致；未部署。
- 新增离线 V0 验收计算：失败/放弃/仅导出不从分母剔除，文字与音频分列，实际请求/未知费用独立记录；22 项合成数据测试通过。空模板退出码 1 是预期未达门槛，不是工具故障；未制造真实测试次数。
- 多标签覆盖风险进一步实现为同源单写入窗口：持有浏览器独占锁才允许保存或删除 Session；第二窗口等待、关闭首窗口后自动接手，录音库仍允许播放和下载。无支持/拒绝锁时停止编辑，数据格式不迁移。
- 依据 W3C Web Locks 规范的独占与窗口卸载释放语义实现，核对了本地 Next 客户端文档；没有新增依赖。
- 38 项状态/回归测试通过。类型检查发现测试类型 LockGrantedCallback 缺泛型参数，已补 unknown 后复验。
- 新的独立浏览器 API 测试首次通过 5 项：阻止第二窗口写入/删除、关闭首窗口交接、关闭等待窗口不堵队列、刷新后重获锁且记录保留、释放后拒绝写入。证据 .codex/qa/session-writer-1790228950183/report.json，含实际源码 SHA-256；全部为临时 Edge 配置、本机测试源、合成记录，无 Provider 调用。
- 此独立检查没有重跑此前失败两次的全应用录音浏览器脚本；也不冒充手机/完整 UI 验收。实际应用和手机多窗口仍待验。

## 2026-09-24T13:54:15 返回缓存页与验收材料
- 页面离开立即撤销本页写权限，返回缓存页须重新排队获取锁；新增生命周期测试通过，防止旧页返回后继续写入。
- V0 证据说明、空模板和统计工具完成；明确真实设备、失败分母、实际分片重试、未知费用和 72 小时证据口径。

## 2026-09-24T13:57:46 第二批完整验证
- 26 文件、191 项自动测试通过；TypeScript 与生产构建通过；差异空白检查通过。
- 加入前进后退交接后的隔离 Edge 检查再次通过，共 6 项；最终证据 .codex/qa/session-writer-1790229164015/report.json。这里是新增生命周期测试，不是重复运行已失败的旧全应用测试。
- 多窗口覆盖缺口的代码保护已完成；真实应用/手机待验，验收模板保留该待验问题，未提前宣称 V0 达标。
- 接下来精确提交本批源码、离线验收脚本及文档，并推送独立分支；人工部署、真机、Redis 和账单待用户回来处理。

## 2026-09-24T13:59:42 推送与待人工阶段
- 第二批业务提交 6edb51aa3628de85af5efa0e3e213aac4a33e7d3 已推送 codex/cloudbase-run-test，远端 SHA 一致；main 未修改。
- 当前线上仍按用户已验收的 2aeac3e 记账，本轮没有部署。下一步需要用户回来后部署新包，再验单窗口启动、双窗口交接、草稿/原音、STT 处理中刷新和旧录音。
- 真实 Redis、设备/浏览器覆盖、50 次样本、三天稳定窗口与账单尚缺人工证据；不向无人值守用户提问，不新建凭据，不启用未验备用或后续模式。
- 新的隔离浏览器辅助服务已退出，未执行关机。此前浏览器完整录音故障矩阵仍跳过，保留失败证据。
- 本日志随分支提交；最终部署 ZIP 清单、哈希、提交状态及下一条命令另写入 .codex/qa/deployments 与新交接文件。

## 2026-09-24T16:51:58+08:00 真机反馈修复：恢复说明不再被题目朗读清空
- 用户在 vivo X100s Pro / Chrome 上确认 104 秒录音刷新后完整，刷新前尚未出现文字，恢复后文字仍空；本轮不记为文字丢失，也不冒充成功分片恢复验收。
- 已复现提示缺陷：恢复说明使用 notice，随后自动题目朗读共用同一 setter，成功或本机朗读会将其清空。将题目朗读提示分离到 questionVoiceNotice，并显示于题目卡片。
- 校验原音且存在结果未确认的分片时，分别说明“尚无保存文字”或“已有文字仍保留”，给出手动填写/保留录音继续的路径。四种界面语言同步。原音验证失败时不声称录音已保留。
- 3 个新增回归覆盖空文字恢复后云端朗读、部分文字恢复后本机朗读、原音验证失败。初次 2 项预期失败复现旧行为，修复后针对性 36 项通过；完整 26 文件、194 项通过，TypeScript、生产构建通过。
- 生成补丁首次因日语键未加引号的匹配断言中止，未写入源文件；按已读源码修正后一次重试成功。未为解决问题更改依赖或凭据。
- 保存格式、请求重试/计费保护、模型配置均不变。未调用线上模型、未部署；8 个用户原有修改哈希一致，main 未操作。提交前仅暂存本轮 4 个源码/测试/验收文档文件。
- 目标：推送独立分支并提供新归档。部署后直接恢复此次旧草稿，确认中断说明在题目自动/手动朗读后仍存在；无需用户重录。长期/其他设备、真实 Redis、账单、50 次/3 天门槛继续待验。
