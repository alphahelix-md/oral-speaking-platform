# ORAL 午后无人值守日志 — 2026-09-24

## 2026-09-24T13:03:54+08:00 启动与用户验收
- 授权窗口：上海时间 12:58:52 至 14:58:52；不向用户申请，故障最多两次重试后记录跳过。
- 用户确认新入口正常、录音停止/处理后刷新恢复可完整播放。当前部署业务基线 2aeac3e，设备/浏览器名称未确认；不推算所有设备或整场完成且保留率。
- 实际分支 codex/cloudbase-run-test；本轮只在该分支开发、测试、提交、推送。部署、采购、登录、新凭据留待用户，不关机，不输出秘密。
- 保留 .gitignore、package.json 及八份用户/并行文档修改，基线哈希位于 .codex/qa/unattended-pm-baseline.json；不整体暂存这些文件。
- 最新计划新增英语六模块规范，但 V0 可靠性仍是前置；不因此开启新模式、备用、专业评分或云同步。
- 已核对具体请求路径：客户端 STT 无截止时间；评价超时只覆盖响应头，响应体卡住仍可使处理状态无限等待。优先修此实际 P1 故障边界。
- 真实 Redis、其他设备、50 次样本和三天门槛无法由本轮离线自动测试替代，继续待验。

## 13:10–13:17 请求截止与旧录音保护
- 客户端统一截止时间覆盖异步认证、响应头及正文；转写 60 秒、评价 25 秒，超时释放界面锁且不自动重发。首轮针对性 18 项通过。
- 首次类型检查发现异步闭包对可空 current 的收窄失效，改为捕获已保存的不可空快照，随后复验。
- 旧录音重试入口原本只保存成功文字，刷新/连点可重发；新增 IndexedDB 同事务领取分段、持久请求 ID、未知结果阻止重发及点击锁。成功旧文字兼容迁移，不删除录音。
- 元数据保存不再把“录音已不存在”当成成功；条件冲突中止事务。
- 两次读取使用不存在的猜测路径（lib/audio-storage.ts、core/session/types.ts），改为已发现的 core/audio/store.ts、types/speaking.ts；无文件修改。vitest 配置实际为 .mjs。

## 13:18–13:24 预算一致性与服务器截止
- 类型检查通过；完整离线回归 24 文件、148 项通过。
- 路由原先主模型两次再备用一次，与硬预算最多两次冲突。统一共享上限为 2：无备用时主模型最多两次；将来获准备用时主/备用各一次；本轮仍不配置备用。
- 新增可取消的整体截止：评价每次最多 10 秒、整体 21 秒，转写整体 45 秒；预算预留、账目写入、模型请求及正文共享信号。过期的预算结果不能再启动模型。
- 首次生成补丁被 Windows 默认 GBK 读取中文源码阻止，尚未写入任何文件；显式 UTF-8 后第一次重试成功。

## 13:25–13:31 本批复核
- 路由故障注入通过；最终账目 HSET 改为有时限的尽力写入，不阻塞已收到模型结果。预留记录为权威计费凭证，完成标记中断仍保留 reserved，不能当作免费调用。新增记账卡死回归通过。
- 24 文件、158 项完整测试通过；类型检查、生产构建通过；Lua 离线提取校验通过，网络调用 0。构建后又补了记账及回放错误处理，提交前再次统一构建。
- 历史录音已知“请求未发送”的认证/访问码拒绝保留安全重试；未知网络结果仍阻止重发。
- 已读回验证的原音不再被可选播放缓存读取失败标成丢失；历史答案回放的存储/播放拒绝显示提示并释放临时 URL。
- 审阅发现现有 Session localStorage 修订号保护能拒绝陈旧页，但不构成两个标签页同时写入的原子事务；保留为待解决 V0 限制，不将旧录音的 IndexedDB 原子领取误表述为全站跨标签原子保护。

## 13:32–13:34 认证迟到复核
- 将整体截止起点前移到服务器认证/上传解析之前，并传递客户端取消信号。迟到认证或预算响应不能在用户已超时后启动收费模型。
- 新增“认证卡住后恢复”“请求进入时已取消”路由测试；针对性 26 项通过。没有线上请求、凭据或配置变更。
- 本轮全部改动只涉及 V0 可靠性；备用、模式、依赖和服务开关均未新增。

## 2026-09-24T13:34:57 本批提交验证
- 24 个测试文件、163 项通过；TypeScript 与最终生产构建通过；git diff --check 通过。
- 本轮没有调用收费模型，没有改变部署、主分支、环境配置或依赖；8 个原有已修改文件哈希全部一致。
- 接下来只暂存本轮可靠性文件，提交并推送 codex/cloudbase-run-test。
