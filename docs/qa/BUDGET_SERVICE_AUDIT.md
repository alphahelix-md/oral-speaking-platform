# 预算与服务故障核验（2026-09-24）

本轮在 `codex/cloudbase-run-test` 继续 V0 可靠性工作。检查起点为 dd68e7e；线上最后经用户部署并核对前端的版本仍为 bf7feac。本轮朗读修复需要另行部署，本文不代表线上验收通过。

## 已核对的边界

| 项目 | 代码/隔离测试证据 | 仍未验证 |
| --- | --- | --- |
| STT 与评分预算 | 供应商调用前预留；账号、全局、Session 三层；调用次数、PCM 音频时长、估算费用；同一操作最多两次实际尝试；重复请求拒绝 | 真实 Redis Lua 执行、并发额度边界、线上配置和账单 |
| 预算服务故障 | 配置无效、预算读取/预留失败时阻断付费调用；迟到结果不能启动已取消请求 | 真实 Redis 故障及线上端到端验证 |
| 完成回执 | 后台尽力写入，失败不能丢弃已收到结果或触发重试 | 供应商账单对账；未完成回执不代表零费用 |
| 评分与转写等待 | 评分服务端 21 秒、转写 45 秒总时限；既有测试覆盖迟到鉴权、响应体卡住、预算取消 | 真实供应商故障与其他终端 |
| 题目朗读等待 | 本轮改为从鉴权开始统一 15 秒时限；读取正文、初始化和收完整音频共用时限；客户端取消关闭连接/音频流；迟到初始化不再启动合成 | 新版部署后真实题目播放；云端断连信号传递 |

预算源码、测试和运行脚本已核对；没有启用备用模型，没有开展付费模型筛查，也没有更改预算限额或部署环境变量。TTS 仍只使用原有访问控制和客户端缓存，未纳入 STT/评分的估算费用预算。

## 本轮修复与证据

旧 TTS 时限仅包围合成阶段，鉴权或读取请求卡住会超出时限；客户端取消不传递，初始化迟到后仍可能开始合成。修复复用 `withDeadline`，在外部工作开始前检查取消，丢弃未完整返回的音频，并释放流/连接。原有声音、语速、输入校验、私有缓存及错误响应保持不变。

新增 `app/api/tts/route.test.ts` 共 16 项：英/日四种声音配置、鉴权拒绝、无效输入、已取消请求、鉴权/正文卡住、初始化迟到、半段音频卡住、取消、空音频及服务错误。模拟鉴权和朗读库，不发真实模型请求。

- 修复前：16 项中 9 通过、7 失败，保留失败证据。
- 修复后：16/16 通过。
- 全量回归：28 个测试文件、232/232 通过。
- TypeScript 检查和 Next.js 生产构建通过。
- 本轮无界面修改，不重新把已有布局/真机结果计成新增样本。

私有证据位于 `.codex/qa/budget-service-audit/`：`tts-before.json`、`tts-after.json`、`unit-tests.json`、`build.log`、`local-config-presence.json`。

## 预算核验缺口

只检查了本地环境的配置存在性，没有输出密钥。未发现专用 `ORAL_REDIS_TEST_URL` / `ORAL_REDIS_TEST_TOKEN`，本机 PATH 没有 Redis 或 Docker；未创建外部服务、新增凭据或使用生产用户计数器。当前本地 `.env.local` 中预算配置缺失，**不能据此判断 CloudBase 线上配置**。

`node scripts/test-budget-redis.mjs --check` 离线检查通过，只验证源码 Lua 提取和脚本结构，网络请求为零；Lua SHA256 为 `258615609f58b29678fb0e944e3ac5f2d60aae69da3bfb880c3a9037b0d7c51e`。真实数据库 12 项场景仍未执行。

后续条件：由本人提供已授权的独立测试库环境，再运行已有 Redis 检查；核对线上运行模式及限额；依据实际服务价格/调用量对账。费用预留是估算值，不能宣称账单硬上限或完整 Cost Governor 已启用。缺少配置时保留待办，不填猜测数值。

电脑转写 QA-001、无 iPhone 设备 QA-002 继续按用户要求暂缓。50 次跨设备尝试、三天稳定性、自动停止录音和真实存储满等发布门槛仍未完成。
